const index = parseInt(process.argv[2]);
const arr = [];

for (let i = 0; i < index; i++) {
  arr.push(i);
  arr.includes("i");
  //   console.log(i + 500 * 1);
}
arr.concat(arr).reverse().reverse().reverse().reverse().reverse().reverse();
