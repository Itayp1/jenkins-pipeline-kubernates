apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "example2"
  namespace: "qa"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "example2"
  template:
    metadata:
      labels:
        app: "example2"
    spec:
      volumes:
        - name: example-volume
          persistentVolumeClaim:
            claimName: example-volume-claim
      containers:
        - name: "example"
          image: "itayp/example:1.0.13"
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: "/health"
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
          env:
            - name: "SERVICE_NAME"
              value: "example2"
          envFrom:
            - configMapRef:
                name: example-config-map
            - secretRef:
                name: example-secret
          volumeMounts:
            - name: example-volume
              mountPath: /var/storage
