apiVersion: "v1"
kind: "Service"
metadata:
  name: "example2-service"
  namespace: "qa"
spec:
  type: "ClusterIP"
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: "example2-deployment"
