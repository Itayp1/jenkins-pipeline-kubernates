apiVersion: "v1"
kind: "Service"
metadata:
  name: "hello-world-test"
  namespace: "qa"
spec:
  type: "ClusterIP"
  ports:
    - port: 443
      targetPort: 3000
  selector:
    app: "hello-world-test"
