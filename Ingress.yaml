apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "example1"
  namespace: "qa"
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: "example1-qa.digital-cloud-services.com"
      http:
        paths:
          - pathType: "Prefix"
            path: "/"
            backend:
              service:
                name: "example1"
                port:
                  number: 443
